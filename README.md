#setup and config
1. "flutter pub get" in command to install dependencies
2. "flutter run" to run application

#build artifact
1."flutter build apk --release" 
2.when success building the file will show at build/app/outputs/flutter-apk/app-release.apk
